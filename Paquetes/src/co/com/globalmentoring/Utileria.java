package co.com.globalmentoring;

public class Utileria {

    //Si creamos un método estático para imprimir una cadena que se recibe por parámetros:
    public static void imprimir(String s) {
        System.out.println("s = " + s);
    }
}
